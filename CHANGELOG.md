# Changelog

## 0.2.0 - 2026-03-28

- modernize the library build with `tsup`, ESM/CJS exports and TypeScript 6
- add test coverage with Vitest and jsdom for scroll synchronization behavior
- remove the `lodash/throttle` dependency and harden edge cases around zero overflow
- update the package metadata, peer dependency range and Yarn-based project scripts
- replace the legacy demo with a Vite + React 19 app and modernize GitHub Pages deployment
